import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.reactCounterShopping',
  appName: 'react-counter-shopping',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
